class Calculator {

    solve(operation) {
        var result = Global.eval(operation)
        var display = document.getById("display")
        display.innerHTML = display
    }

    sum() {
        document.getById("display").append("+")
    }

    sum() {
        document.getById("display").append("+")
    }
}
